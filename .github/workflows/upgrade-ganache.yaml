name: Upgrade ganache version
on:
  workflow_dispatch:
  schedule:
    # every 1st and 16th of the month at 3am UTC
    - cron: "0 3 1,16 * *"
jobs:
  upgrade:
    name: Upgrade ganache version
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/setup-node@v1
        with:
          node-version: 16.1
      - name: Upgrade ganache version
        uses: technote-space/create-pr-action@v2
        with:
          GITHUB_TOKEN: ${{ secrets. GH_BOT_TOKEN }}
          EXECUTE_COMMANDS: |
            npm install -g npm-check-updates@^16.3.2
            cd packages/waffle-provider
            ncu ganache -u
            cd ..
            cd waffle-ens/
            ncu ganache -u
          COMMIT_MESSAGE: "🍩 Upgrade ganache version"
          COMMIT_NAME: "truefibot"
          COMMIT_EMAIL: "truebot.github@trusttoken.com"
          PR_BRANCH_NAME: "chore-upgrade-ganache-${PR_ID}"
          PR_TITLE: "🍩 Upgrade ganache version"
