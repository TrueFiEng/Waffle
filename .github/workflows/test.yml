name: Optimism testing

on:
  push:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: First
        run: cd waffle-optimism
      - name: Second
        run: pwd
